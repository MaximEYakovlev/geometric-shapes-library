/**
 * Interface describes a polygon.
 */
export interface IPolygon {
    /**
     * Returns the length of the shape sides.
     * @returns {number[]} Array of shape sides length.
     */
    getSidesLength(): number[];
}
